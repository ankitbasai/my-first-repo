---
      - name: check the server
        hosts: all
        tasks:
               - name: create user
                 user:
                       name: ankit
                       state: present
